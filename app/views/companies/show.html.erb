<div class="container mt-5 mb-5">
  <div class="row justify-content-center">
    <div class="col-20">
      <img src="<%= cl_image_tag(@company.logo.key, width: 200, height: 200, crop: :fill) %>" alt="Company Logo">
      <h1>Details of <%= @company.name %></h1>

      <p>Name: <%= @company.name %></p>
      <p>Headquarters: <%= @company.headquarters %></p>
      <p>Industry: <%= @company.industry %></p>
      <p>Description: <%= @company.description %></p>
      <p>Contact info: <%= @company.contact_info %></p>
    </div>
  </div>
</div>


<%= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY",
      width: 400, height: 300, crop: :fill) %>
